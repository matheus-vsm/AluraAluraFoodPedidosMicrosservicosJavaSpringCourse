spring.application.name=pedidos-ms

# Conexão com MySQL (Docker: use as credenciais padrão abaixo ou defina DB_USER_MYSQL e DB_PASSWORD_MYSQL)
spring.datasource.url=jdbc:mysql://localhost:3306/alurafood_pedidos?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=${DB_USER_MYSQL:alurafood}
spring.datasource.password=${DB_PASSWORD_MYSQL:alurafood123}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

#server.port=0
#eureka.client.serviceUrl.defaultZone=http://localhost:8081/eureka
#eureka.instance.instance-id=${spring.application.name}:${random.int}